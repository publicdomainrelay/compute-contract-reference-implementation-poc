# Non-secret config + secret STUBS. Committed. Real secrets in
# market-registry.env.secrets (gitignored), loaded after this file so they
# override the stubs below.
PORT=4031
PLC_DIRECTORY_URL=https://plc.directory
DISPATCHER_HOST=xrpc.fedproxy.com
BASE_ORIGIN=https://market-registry-0001--johnandersen777-bsky-social.fedproxy.com
ATPROTO_HANDLE=johnandersen777.bsky.social
ATPROTO_USERNAME=johnandersen777.bsky.social
ATPROTO_DID=did:plc:5svqtrhheairglgiiyvutzik
ATPROTO_PDS=https://morel.us-east.host.bsky.network

# --- secret stubs (override in market-registry.env.secrets) ---
# Stable repo signing key; if unset registry generates an ephemeral one each boot.
REPO_PRIVATE_KEY_HEX=CHANGE_ME
ATPROTO_PASSWORD=CHANGE_ME
ATPROTO_APP_PASSWORD=CHANGE_ME
