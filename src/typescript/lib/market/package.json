{
  "name": "@publicdomainrelay/market",
  "version": "0.0.0",
  "description": "Helpers for the com.publicdomainrelay.temp.market.* lexicons and their submit* XRPCs (server handlers + client).",
  "type": "module",
  "exports": {
    ".": "./mod.ts"
  },
  "peerDependencies": {
    "@atproto/api": "^0.20.8",
    "@atproto/common-web": "^0.5.0",
    "@atproto/identity": "^0.5.0",
    "@atproto/xrpc": "^0.7.0",
    "@atproto/xrpc-server": "^0.11.1"
  },
  "comments": {
    "node": "Sources are TypeScript with explicit .ts import specifiers (Deno convention). Node consumers should load via a TS-aware loader (tsx / ts-node ESM) or run the files through a bundler/tsc that preserves the .ts resolution. The runtime code uses only web-standard Request/Response plus the @atproto/* packages above — no Deno.* or node:* APIs."
  }
}
